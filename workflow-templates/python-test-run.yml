name: Run Hello World

on:
  push:
    branches: [ $default-branch ]
  pull_request:
    branches: [ $default-branch ]
  workflow_dispatch: #maunual trigger

jobs:
  pytest: 
    runs-on: ubuntu-latest
    steps:
      - name: Run echo
        run: echo Hello from Worklow Template



    
  

    

    

